# This file holds sops encrypted secrets for the cluster indicated in the directory name.
# It is meant to be used with helm-secrets as an argument to helm in the form of `-f secrets.yaml`
---
authentication:
  clientId:
  clientSecret:
